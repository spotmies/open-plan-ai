// Shared UI components available to all modules.
// ui/ primitives are imported directly from @/components/ui/* (they are thin shadcn wrappers).
export { ErrorBoundary } from '@/components/ErrorBoundary';
export { SuspenseFallback } from '@/components/SuspenseFallback';
export { AppLayoutSkeleton } from '@/components/layout/AppLayoutSkeleton';
