// Bridge re-export — this file is kept for backwards compatibility during migration.
// All code should progressively move to importing from '@/modules/auth'.
export { AuthProvider, AuthContext, useAuth } from '@/modules/auth/AuthContext';
